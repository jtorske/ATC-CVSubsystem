# 📸 AprilTag Detection with OpenCV + Python  

This project demonstrates **real-time AprilTag detection and pose estimation** using **OpenCV** and **pupil-apriltags** in Python.  
The script opens your webcam, detects AprilTags (`tag36h11`), and displays their **ID, position, and orientation**.  

---

## 🚀 Dependencies  

Install the following Python packages:  

```bash
pip install opencv-python numpy pupil-apriltags
